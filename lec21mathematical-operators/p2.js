// naming convention -> the name should be self explantory (i mean variables.)
// the varible name cannot start with a number.
// a variable cannot start with symbols.
// but there are some exception $ , _
let $name="anadi";
let _lastname="kumar";
console.log($name,_lastname);
let a="anadi";
a=5;
console.log(a);
// you cannot usse space beetween varible naming.
// there are two ways of wriiting a variable name.
// 1. camelcase -> first word need not to start with captal letters.
let myName="Anadi"; //-> camelCase
let myFatherName="anil kumar"; //-> camelCase

// 2. sankecase -> seprating variable name with underscore.,each word is seprating by  a _ (unserscore).
let my_name="Anadi";
let  my_father_name="anil kumar";
